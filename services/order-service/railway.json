{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npx prisma generate --schema=services/order-service/prisma/schema.prisma && cd services/order-service && npx tsc",
    "watchPatterns": ["services/order-service/**"]
  },
  "deploy": {
    "startCommand": "node services/order-service/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
